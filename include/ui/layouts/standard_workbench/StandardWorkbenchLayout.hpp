#pragma once

#include "app/AppState.hpp"
#include "app/UiActions.hpp"

namespace gs3d::ui {

/*
 * =========================================================================================
 * 方案 A · 标准工作台布局入口 (Standard Workbench Layout)
 * =========================================================================================
 * 
 * 布局形态：三栏独立流式工作台 (上下文栏 + 左侧数据/导航 + 中央3D视口 + 右侧属性检查器 + 状态栏)
 *
 * +---------------------------------------------------------------------------------------+
 * | [顶部上下文栏] 数据集名称 | 截屏 | 测量模式开关                                       |
 * +-----------------------+---------------------------------------+-----------------------+
 * | [左栏: 场景/导航]     | [中栏: 3D 实时点云视口]               | [右栏: 属性/检查器]   |
 * | - 数据集列表          | - 视角快捷工具条                      | - 点渲染大小/颜色模式 |
 * | - 鸟瞰空间导航图      | - 3D 点云主画布                       | - 色带LUT/直方图/剪裁 |
 * +-----------------------+---------------------------------------+-----------------------+
 * | [底部状态栏] 当前状态、已加载点数、GPU显存与上传预算                                  |
 * +---------------------------------------------------------------------------------------+
 */
void draw_standard_workbench_layout(
    gs3d::app::AppState& state,
    gs3d::app::UiActions& actions,
    float ui_scale
);

} // namespace gs3d::ui
